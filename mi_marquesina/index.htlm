<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <title>Marquesina Portafolio</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f4fff4;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    /* Contenedor de la marquesina */
    .marquesina {
      width: 100%;
      background-color: #388E3C; /* Verde oscuro */
      overflow: hidden;
      padding: 30px 0;
      position: relative;
    }

    /* Texto que se mueve */
    .marquesina span {
      display: inline-block;
      white-space: nowrap;
      color: white;
      font-size: 36px;
      font-weight: bold;
      animation: deslizar 10s linear infinite;
    }

    /* Animación */
    @keyframes deslizar {
      from {
        transform: translateX(100%);
      }

      to {
        transform: translateX(-100%);
      }
    }
  </style>
</head>

<body>

  <div class="marquesina">
    <span>¡Bienvenidos a mi Portafolio! 🌟</span>
  </div>

</body>

</html>